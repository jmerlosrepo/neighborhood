export enum textInputType {
  text = "text",
  password = "password",
}

export enum buttonType {
  submit = "submit",
  button = "button",
}

export enum userType {
  manager = "manager",
  neighbor = "neighbor",
}
