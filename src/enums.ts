export enum textInputType {
  email = "email",
  number = "number",
  password = "password",
  text = "text",
}

export enum buttonType {
  button = "button",
  submit = "submit",
}

export enum userType {
  manager = "manager",
  neighbor = "neighbor",
}
